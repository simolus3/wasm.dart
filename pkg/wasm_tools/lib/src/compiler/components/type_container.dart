// TODO: Remove
import 'type.dart';

final class TypesContainer {
  ModelTypeReference addFunctionType(FunctionType type) {
    throw 'todo: remove';
  }

  ModelTypeReference addInstanceType(InstanceType type) {
    throw 'todo: remove';
  }

  ModelTypeReference addValueType(ValueType type) {
    throw 'todo: remove';
  }
}
