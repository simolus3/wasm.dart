name: integration_tests
publish_to: none
resolution: workspace

environment:
  sdk: ^3.14.0-0

dependencies:
  hooks: ^2.2.0
  meta: ^1.19.0
  wasi:
  wasm_components:
  wasm_tools:

dev_dependencies:
  dart_style: ^3.1.13
  logging: ^1.3.0
  pub_semver: ^2.2.1
